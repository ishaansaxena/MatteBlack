// Retrieve data
let d_o = sdata["1. open"];
let d_h = sdata["2. high"];
let d_l = sdata["3. low"];
let d_c = sdata["4. close"];
let dates = sdata["dates"];

// Layout definitions
const margin = {
    l: 50, r: 50,
    b: 50, t: 50,
    pad: 0
}

// Colors
const colors = {
    positive:   "#00796B",
    negative:   "#D64541"
}
